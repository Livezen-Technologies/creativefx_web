; Allow large media uploads (launch video MP4s) through the admin uploader.
; Read by PHP-FPM per-directory; harmless where ignored.
upload_max_filesize = 512M
post_max_size = 520M
max_execution_time = 300
memory_limit = 512M
